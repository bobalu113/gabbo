/**
 * The /obj package is for programs which are meant to be instanced, rather 
 * than inherited. Here you will find programs for objects that are 
 * integrated into the game's diegesis (e.g. money), or function as service 
 * and support objects (e.g. a calendar). 
 */
