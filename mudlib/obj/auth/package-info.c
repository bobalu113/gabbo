/**
 * These objects are used authentication and authoratization. Generally, 
 * any business logic which constrains what a user has access to should go in 
 * an auth/ package. The /obj/auth/ package specifically constrains global 
 * access.
 */
