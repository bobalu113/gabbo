/*

    Setting: prompt.format
       Type: string
       Zone: None
    Default: >
      Value: No value configured.
Description: Sets the default prompt format.
             etcetc

types: boolean, integer, decimal, string, list(strings), map(strings)

set -z n : list all active settings going n zones back
set -a : list all active settings from all descendent zones
set -p : list personal setting values (active settings only)
set -P : list all personal setting values
set -h setting.name : display help
set setting.name : display configured and/or active value
set setting.name value : set new value
set -u setting.name : unset setting

 */